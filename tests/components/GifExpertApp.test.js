import { render, screen } from "@testing-library/react";
import { GifExpertApp } from "../../src/components/GifExpertApp/GifExpertApp";

describe("testing GifExpertApp", () => {
  test("should ", () => {
    render(<GifExpertApp />);
    screen.debug();
  });
});
